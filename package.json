{
  "name": "karate",
  "version": "0.0.1",
  "scripts": {
    "start": "grunt fastbuild && pushd build && node server && popd"
  },
  "dependencies": {
    "express": "~3.0.0"
  },
  "devDependencies": {
    "grunt": "~0.3.0",
    "grunt-html": "~0.2.0",
    "grunt-css": "~0.3.0",
    "grunt-contrib-copy": "~0.3.0",
    "grunt-contrib-clean": "0.3.1",
    "grunt-contrib-compress": "~0.3.0"
  }
}
